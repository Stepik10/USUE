class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
       
        anwser=[]
        i= len(nums)
        for x in range (i):
            for y in range(x+1,i):
                if (nums[x]+nums[y]==target):
                    return [x,y]

